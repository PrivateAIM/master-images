# httpx/fastapi/uvicorn are capped by flamesdk 0.6.0 (pulled in via
# python-sdk-patterns 0.6.5): httpx ^0.27.0, fastapi ^0.110.0, uvicorn ^0.27.1.
# These are already the highest versions inside those ranges - do not bump
# them without bumping the SDK first.
httpx == 0.27.2
fastapi == 0.110.3
uvicorn == 0.27.1
minio == 7.2.20
pandas == 3.0.5
torch == 2.13.0
torchvision == 0.28.0
torcheval == 0.0.7
scikit-learn == 1.9.0
numpy == 2.5.2
tenseal == 0.3.17
matplotlib
tqdm
pudb
opacus
